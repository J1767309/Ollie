
import {
  BookOpen,
  Zap,
  CheckCircle,
  XCircle,
  ArrowRight,
  Award,
  RotateCw,
  type LucideProps,
} from 'lucide-react';

// This file is a workaround for the web-based runtime environment.
// It re-exports the icons used in the application.
// This is a common pattern when a module bundler or environment
// has trouble with direct imports from a large library like lucide-react.

export {
  BookOpen,
  Zap,
  CheckCircle,
  XCircle,
  ArrowRight,
  Award,
  RotateCw,
};

export type { LucideProps };
